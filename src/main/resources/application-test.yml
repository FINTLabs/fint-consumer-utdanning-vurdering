spring:
 application:
  name: fint-consumer-utdanning-vurdering

fint:
 events:
  orgIds: mock.no, mock2.no, mock3.no
  test-mode: true
  queue-endpoint-enabled: true

 audit:
  test-mode: true

 relations:
  force-https: false
  default-base-url: /

 hazelcast:
  members: localhost

server:
 context-path: ""
 port: 8080
